<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
</head>
<body>
    <h1>Edit Product</h1>
   <form action="/products/<%= productSelect.id %>?_method=PUT" method="post">
    <label for="name">Name</label>
    <input type="text" name="name" id="name" value="<%= productSelect.name %>">
    <label for="price">Price</label>
    <input type="number" name="price" id="price" value="<%=productSelect.price%>">
    <label for="category">Select Category</label>
    <select name="category" id="category">
        <% for (let category of categories) {%> 
            <option value="<%= category %>" <%= productSelect.category === category ? "selected" : "" %> ><%= category %></option>
            <% } %> 
    </select>
    <button type="submit">Update</button>
</form>
    
</body>
</html>